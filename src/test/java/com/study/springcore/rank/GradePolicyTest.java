package com.study.springcore.rank;

import com.study.springcore.domain.Member;
import com.study.springcore.service.rank.GradePolicy;
import org.assertj.core.api.Assertions;
import org.junit.jupiter.api.Test;

import java.util.ArrayList;

class GradePolicyTest {
    GradePolicy gradePolicy = new GradePolicy();
    @Test
    void score() {
//        Member member1 = new Member(1L, "user1", 100, 1, 98);
//        Member member2 = new Member(2L, "user2", 80, 2, 85);
//        ArrayList<Member> members = new ArrayList<>();
//        members.add(member1);
//        members.add(member2);
//        int rank = gradePolicy.score(member1, members);
//        Assertions.assertThat(rank).isEqualTo(1);
    }

}